# 第三周作业：Agent v0

- 姓名：吴承儒
- 学号：23230618
- 实验日期：2026-09-25

## 运行环境

复用第一周 `.venv/Scripts/python.exe` 与 `models/Qwen2.5-1.5B-Instruct`，未新建环境、未更换或下载模型。九次真实实验均使用同一个本地模型、CUDA 与 `torch.bfloat16`。

- GPU：NVIDIA GeForce RTX 4060 Laptop GPU，标称 8GB；驱动 581.15。
- Python 3.12.1；PyTorch 2.4.0+cu121；Transformers 4.51.3；Pydantic 2.11.7；OpenAI 1.109.1。PyTorch 使用 CUDA 12.1，`nvidia-smi` 的 CUDA 13.0 表示驱动支持的版本，两者含义不同。
- PyTorch 检测 CUDA、BF16 均可用，各次加载前可用显存 7,441,743,872 字节（约 6.93GiB）。
- 九次运行的最大 PyTorch allocated 峰值为 3,173,543,424 字节（约 2.96GiB），reserved 峰值为 3,395,289,088 字节（约 3.16GiB），来自第 2 次实验。这是当前进程的 PyTorch 显存统计，不包含桌面和其他进程；逐次数据见 JSON 的 `environment`。
- 1.5B BF16 权重粗估约 3GB，实测推理显存余量充足。3B BF16 权重粗估约 6GB，叠加缓存和运行开销后余量较小；7B BF16 权重约 14GB，无法完整放入这块显卡。本次没有实测 3B/7B，也没有采用量化或 CPU 卸载。

## 5 次运行对照

固定任务：`读取 test_report.txt，计算测试通过率`。工作目录 `workspace` 中的报告始终为：

```text
passed=47
failed=3
```

正确计算为 `47/(47+3)*100 = 94%`。固定 `seed=42`、`max_steps=6`、`max_failures=2`、`max_new_tokens=256`，五次均显式使用 `--json-stop legacy`。每次独立进程重新加载模型并设置种子，依次仅修改 sample、temperature、top-p、top-k；输出文件名变化不属于实验变量。贪心时三个采样参数不生效。

以下完整命令在第三周目录执行，使用第一周 Python 的相对路径。实际执行的绝对路径命令还保存在 `logs/commands.json` 和各次日志中。

| 序号 | 调整的参数 | 完整命令 | status | answer | 对应结果文件 |
| --- | --- | --- | --- | --- | --- |
| 1 | 基线：贪心 | `& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '读取 test_report.txt，计算测试通过率' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'legacy' '--output' 'results/01_greedy_legacy.json'` | ok | 请提供正确的文件路径以便进行后续操作。 | [results/01_greedy_legacy.json](results/01_greedy_legacy.json) |
| 2 | 仅开启 --sample | `& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '读取 test_report.txt，计算测试通过率' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'legacy' '--output' 'results/02_sample_t0.7_p0.8_k20.json' '--sample'` | too_many_failures | null（无答案） | [results/02_sample_t0.7_p0.8_k20.json](results/02_sample_t0.7_p0.8_k20.json) |
| 3 | 仅 temperature：0.7 → 0.3 | `& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '读取 test_report.txt，计算测试通过率' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'legacy' '--output' 'results/03_sample_t0.3_p0.8_k20.json' '--sample' '--temperature' '0.3'` | ok | 无法完成任务的原因在于无法读取指定的文件。请确认文件是否存在并且具有可读权限。 | [results/03_sample_t0.3_p0.8_k20.json](results/03_sample_t0.3_p0.8_k20.json) |
| 4 | 仅 top-p：0.8 → 0.95 | `& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '读取 test_report.txt，计算测试通过率' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'legacy' '--output' 'results/04_sample_t0.3_p0.95_k20.json' '--sample' '--temperature' '0.3' '--top-p' '0.95'` | ok | 当前环境不支持执行计算测试通过率的功能。建议检查您的代码或环境配置是否正确，并确认是否有足够的权限来读取文件。 | [results/04_sample_t0.3_p0.95_k20.json](results/04_sample_t0.3_p0.95_k20.json) |
| 5 | 仅 top-k：20 → 50 | `& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '读取 test_report.txt，计算测试通过率' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'legacy' '--output' 'results/05_sample_t0.3_p0.95_k50.json' '--sample' '--temperature' '0.3' '--top-p' '0.95' '--top-k' '50'` | ok | 当前环境不支持执行计算测试通过率的功能。建议检查您的代码或环境配置是否正确，并确认是否有足够的权限来读取文件。 | [results/05_sample_t0.3_p0.95_k50.json](results/05_sample_t0.3_p0.95_k50.json) |

五次都有真实 `read_file` 调用尝试，但全部被路径安全校验拒绝，没有一次成功读到文件，也没有执行 `calculator`。第 1、3、4、5 次的 `ok` 只表示生成了合法的 final；它们没有回答 94%，任务均未完成。第 2 次没有最终答案，因连续失败次数超过预算而终止。这里保留失败结果，因为作业重点是调参与 trace 分析。

## 调用链分析

### 第 1 次：贪心，status=ok，但任务未完成

1. **raw** 先说“需要先读取文件并计算测试通过率，请稍等。”，随后输出 `{"action":"tool","name":"read_file","arguments":{"path":"/content/test_report.txt"}}`。**decision** 为 read_file，参数是模型自行补出的绝对形式路径。**observation** 为 `status=error`、`type=execution_error`，消息是 PermissionError，明确要求使用工作目录内的相对路径 `test_report.txt`。尽管 raw 带有额外文字，原版 `parse_decision` 截取 JSON 后仍能解析，所以此步失败来自路径校验。
2. **raw** 为一段道歉和权限建议，还把 `/test_project/test_report.txt` 当成相对路径示例，没有 JSON。因此没有合法 **decision**，trace 记录 `format_error: 输出中没有 JSON 对象`。没有执行工具、没有工具 **observation**；Loop 在 messages 中追加 `Observation: 输出格式错误，请只输出一个符合要求的 JSON 对象。`。失败计数由 1 增至 2，尚未超过预算。
3. **raw** 为 `{"action":"final","answer":"请提供正确的文件路径以便进行后续操作。"}`；**decision** 为 final，answer 保存在原文及顶层结果中。此步不执行工具，因而没有 **observation**。Loop 直接以 `ok` 结束，但并未取得报告数据或给出计算结果。

贪心选取每一步最高概率 token，在此固定模型和 Prompt 下倾向于编造常见目录，并把工具拒绝理解为环境不可用。降低随机性不等于理解路径约束。普通任务的两次拓展对照重复得到相同原文，为这次配置下的行为提供了补充观察。

### 第 2 次：开启采样，status=too_many_failures

1. **raw** 包含较长解释，甚至在没有读取数据前提出“所有测试成功则 100%，有失败则 0%”的错误近似，最后输出 read_file JSON，路径为 `/Users/your_username/Desktop/test_report.txt`。**decision** 仍是合法的 read_file 工具调用；**observation** 为 PermissionError，明确给出工作目录内相对路径示例。失败计数为 1。
2. **raw** 声称“我将使用相对路径来尝试读取文件”，但随后的 **decision** 仍使用完全相同的 `/Users/your_username/Desktop/test_report.txt`。**observation** 再次为相同路径导致的 execution_error，失败计数变为 2。这显示自然语言中的“修正”没有落实到实际 arguments。
3. **raw** 声称已经使用正确相对路径、归因于系统配置并放弃任务，全段没有 JSON。没有合法 **decision** 和工具 **observation**，trace 记录 format_error。累计连续失败达到 3，大于 `max_failures=2`，直接返回 `too_many_failures`，answer 为 null。此时终止发生在格式解析异常分支，不再追加格式纠错 Observation。

与第 1 次相比，只打开采样就改变了路径、解释内容和最终停止状态。此次采样没有改善工具调用，反而出现重复错误调用。不能仅凭一次采样认定温度 0.7 普遍更差；只能确认该固定种子下产生了这条不同轨迹。

### 横向观察

- 第 3 次将温度从 0.7 降为 0.3，第一步路径变成 `/test_report.txt`，依然越出工作目录；随后经过格式纠错给出放弃任务的 final，状态由第 2 次的 too_many_failures 变为 ok，实际任务完成情况没有改善。
- 第 4 次只把 top-p 从 0.8 增到 0.95，路径变成 `/home/user/test_report.txt`，仍然是“路径错误 → 非 JSON 解释 → 放弃任务的 final”。
- 第 5 次只把 top-k 从 20 增到 50，raw、decision、observation 与第 4 次相同，只有耗时等运行数据不同。候选截断参数变化不保证实际采样 token 变化；这里没有记录每步概率，不能断言两个配置的候选集合完全相同。
- 五次主要瓶颈都是模型没有遵守相对路径约束，且未正确利用 Observation 修正参数。自测及工作目录验证支持工具本身可用，模型对“环境不支持”的表述不是事实结论。
- Prompt、模型、任务、种子和停止逻辑都固定，便于观察解码参数带来的差别。但每组只有一次运行，没有进行跨种子统计，也没有大模型对照，不能推出普遍成功率或更大模型一定有效。

## 可选拓展：识别字符串与转义的 JSON 停止条件

### 修改内容与动机

课程原始代码已经有 `_BalancedJSONStop`，本次拓展是修复已有停止逻辑，不将原有功能或 word_count 工具算作新增成果。原逻辑从第一个 `{` 开始无条件数花括号，导致 answer 字符串内的 `}` 被误认为顶层对象闭合；字符串内的 `{` 则可能导致完整对象生成后仍不停。

增加 `in_string` 与 `escaped` 状态：字符串内只处理引号和反斜杠，不计入花括号深度；字符串外才统计 `{`、`}`。每次只扫描新生成 token 的解码文本，保留原有 token 级停止方式。新增 CLI `--json-stop legacy|string-aware`，默认 string-aware，legacy 精确保留原始括号计数分支。`make_hf_llm` 增加同名参数；新增 `--seed` 和结果中的 config/environment 元数据用于实验记录，不改变 Agent 决策协议和工具校验。

该检测只确定对象边界，不代替 JSON/Pydantic 语法与字段验证，也不裁剪或重写模型 raw。停止以 token 为粒度，单个 token 若含边界后的字符仍会保留。首次 `{` 前出现含花括号的说明文字、批量生成、多对象同 token 等情况不属于本次小拓展解决范围。

### 确定性测试

`test_json_stop.py` 的六项 unittest 全部通过：覆盖完整对象每一个未完成前缀、嵌套对象、字符串内左右花括号、转义引号/反斜杠、无对象与不完整 JSON、代码围栏与连续对象、排除 Prompt、原版提前停止和漏停回归。测试使用字符级假 tokenizer 隔离边界检测逻辑，以下真实模型对照补充验证实际 tokenizer 下的效果。

### 真实模型前后对照

固定贪心、seed=42 和其余参数，每对仅改变 json-stop。普通任务使用同一测试报告；边界任务为 `无需调用工具。请给出 final，answer 必须逐字等于：右花括号 } 是普通字符。`，用于隔离字符串截断问题，不混入五次基础实验。

| 任务 | 停止模式 | status | 步数 | answer | 结果文件 |
| --- | --- | --- | --- | --- | --- |
| 报告通过率 | legacy | ok | 3 | 请提供正确的文件路径以便进行后续操作。 | [results/ext_report_legacy.json](results/ext_report_legacy.json) |
| 报告通过率 | string-aware | ok | 3 | 请提供正确的文件路径以便进行后续操作。 | [results/ext_report_string-aware.json](results/ext_report_string-aware.json) |
| 字符串右花括号 | legacy | too_many_failures | 3 | null（无答案） | [results/ext_brace_legacy.json](results/ext_brace_legacy.json) |
| 字符串右花括号 | string-aware | ok | 1 | 右花括号 } 是普通字符。 | [results/ext_brace_string-aware.json](results/ext_brace_string-aware.json) |

普通任务两种模式的模型原文与决策相同，均因错误路径而未完成任务，修复停止条件对此没有改善。

边界任务的 legacy 第 1 步 raw 为 `{"action":"final","answer":"右花括号 }`，在字符串闭合前停止，Pydantic 报字符串未结束；第 2 步 raw 为 `{"action":"final","answer":"}"`，依然缺少对象闭合并解析失败；第 3 步 raw 为 `{"action":"final","answer":'}`，格式再次错误。三步没有合法 decision，也没有工具 Observation；前两次解析错误产生格式纠错消息，第三次超过失败预算后终止。

string-aware 第 1 步 raw 为 `{"action":"final","answer":"右花括号 } 是普通字符。"}`，完整解析为 final，answer 与要求逐字一致，status=ok，无需工具 Observation。这一对照验证修复确实解决了本次真实模型的字符串截断问题，但不代表提升了所有 Agent 任务的完成率。

### 拓展完整命令

```powershell
& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '读取 test_report.txt，计算测试通过率' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'legacy' '--output' 'results/ext_report_legacy.json'
```

```powershell
& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '读取 test_report.txt，计算测试通过率' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'string-aware' '--output' 'results/ext_report_string-aware.json'
```

```powershell
& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '无需调用工具。请给出 final，answer 必须逐字等于：右花括号 } 是普通字符。' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'legacy' '--output' 'results/ext_brace_legacy.json'
```

```powershell
& '../第一周_LLM推理/.venv/Scripts/python.exe' '-X' 'utf8' 'agent_v0.py' '--backend' 'hf' '--model' '../第一周_LLM推理/models/Qwen2.5-1.5B-Instruct' '--device' 'cuda' '--task' '无需调用工具。请给出 final，answer 必须逐字等于：右花括号 } 是普通字符。' '--max-steps' '6' '--max-failures' '2' '--max-new-tokens' '256' '--workdir' 'workspace' '--seed' '42' '--json-stop' 'string-aware' '--output' 'results/ext_brace_string-aware.json'
```

## 验证与提交文件

修改前后 `agent_v0.py --selftest` 均为 14 项 PASS；停止条件 unittest 为 6 项通过。`verify_results.py` 检查了九次进程正常退出、五次任务相同且相邻只改变一个参数、拓展每对只改变停止模式、trace 与 messages 原文一致，并记录结果文件 SHA-256。验证通过不等于模型完成了通过率任务。所有真实结果由程序直接写出，未手工修改。

```powershell
& '../第一周_LLM推理/.venv/Scripts/python.exe' -X utf8 agent_v0.py --selftest
& '../第一周_LLM推理/.venv/Scripts/python.exe' -X utf8 -m unittest test_json_stop -v
& '../第一周_LLM推理/.venv/Scripts/python.exe' -X utf8 verify_results.py
```

- `agent_v0.py`：停止条件拓展、种子与运行配置/显存记录。
- `提交说明.md`：本报告。
- `results/`：上表列出的五份基础结果及四份拓展结果，保留完整 raw、trace、messages。
- `workspace/test_report.txt`：固定任务输入。
- `test_json_stop.py`、`verify_results.py`：边界测试与实验结果核验。
- `run_experiments.py`：九次实验的顺序运行脚本，使用调用它的 Python；必须用第一周环境启动。已有结果时主动拒绝覆盖，重跑请先将本次结果和日志另存归档。
- `logs/`：修改前后自测、停止条件测试、九次推理 stdout/stderr、完整命令清单和结果核验摘要。

日志保留了现有 Transformers 的缓存变量弃用提示、贪心时模型配置内采样参数不生效的警告，以及 Windows 下注意力实现提示；没有修改注意力实现或过滤这些输出，九次实验使用同一环境和后端。
