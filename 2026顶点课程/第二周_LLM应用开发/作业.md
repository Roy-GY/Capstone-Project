# 第二周作业：LLM 应用开发与结构化输出

## 任务

1. 按 [README](README.md) 跑通示例，观察普通回答、流式输出和结构化校验，保存运行结果。
2. 在示例基础上，自主设计并实现一项小拓展。可以调整 Prompt、尝试其他代码生成任务、修改输出格式或校验规则、改进多轮交互与错误处理，也可以尝试其他想法，方向不限。
3. 保存拓展后的运行结果，简要说明做了什么改动、希望解决什么问题，以及实际效果。

`count_positive` 只是参考示例，不限定最终任务。上述拓展方向不要求逐项完成，完成一项小体量的尝试即可；结果未达到预期时，如实记录问题和观察。

## 提交内容

- 修改后的 `llm_app.py`；如新增少量配套文件，一并提交。
- 运行结果 JSON，覆盖上述任务，保留真实输出及校验结果。
- 按下方模板填写的 `提交说明.md`。

## 提交模板

````markdown
# 第二周作业：LLM 应用开发与结构化输出

- 姓名：<姓名>
- 学号：<学号>

## 运行环境与命令

- 模型、设备与精度：<实际配置>
- 依赖版本：<见结果 JSON 的 environment>

```bash
<实际执行的完整命令>
```

## 拓展与结果

- 拓展内容：<想做什么，为什么选择这个方向>
- 主要改动：<修改了哪些代码或 Prompt>
- 运行结果：<对应文件名，实际效果及验证方式>
- 观察与问题：<与原示例相比有什么变化，是否达到预期>

## 提交文件

- 应用脚本及配套文件：<文件名列表>
- 运行结果：<JSON 文件名列表，并注明示例运行或拓展后的结果>
````
