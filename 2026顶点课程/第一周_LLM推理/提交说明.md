# 第一周作业：Qwen 模型推理

- 姓名：吴承儒
- 学号：23230618

## 运行环境

- 模型：Qwen2.5-1.5B-Instruct
- 设备：NVIDIA GeForce RTX 4060 Laptop GPU
- 精度：`torch.bfloat16`
- Python / PyTorch / Transformers：Python 3.12.1 / PyTorch 2.4.0+cu121 / Transformers 4.51.3

## 运行命令

```powershell
uv run --locked python qwen_inference.py --model ./models/Qwen2.5-1.5B-Instruct --device cuda --max-new-tokens 32 --debug --output results/run.json
```

## 推理结果

运行完成后，完整输入、模型输出、单次生成耗时、实际生成 token 数，以及 hidden states 和 KV cache 的形状与采样值记录在 [results/run.json](results/run.json)。

本次实测使用 RTX 4060 Laptop GPU，CUDA 可用；生成 6 个 token，精确计时为 `0.19475590001093224` 秒，结果文件中的 `debug` 字段包含 prefill `[1,39,1536]`、decode `[1,1,1536]` 和首层 KV cache 形状。

- 完整输入：中国的首都是哪里？请用一句话回答。
- 完整输出：中国的首都是北京。
- `generation_seconds`：`0.19475590001093224`
- `generated_tokens`：`6`
- Hidden states（以下均为 `hidden_states[index][0,-1,:8]`）：
  - 索引 `0`，shape `[1,39,1536]`：`[-0.035888671875, -0.048095703125, 0.0166015625, 0.00836181640625, 0.00872802734375, 0.031982421875, 0.0108642578125, -0.01177978515625]`
  - 索引 `1`，shape `[1,39,1536]`：`[0.224609375, -0.13671875, -0.126953125, 0.02734375, 0.25, -0.020751953125, -0.080078125, -0.1220703125]`
  - 索引 `28`，shape `[1,39,1536]`：`[-0.40625, -1.296875, 1.1484375, 2.125, -5.03125, -3.078125, 0.546875, -3.625]`
- 首层 KV cache：prefill key/value shape `[1,2,39,128]`，decode 后 key shape `[1,2,40,128]`。

## 提交文件

- 推理脚本：`qwen_inference.py`
- 运行结果：`results/run.json`
- 环境配置：`pyproject.toml`、`.python-version`、`uv.lock`

模型权重和虚拟环境未纳入提交。
